LET currentTimestamp = DATE_NOW()
LET twelveHoursAgo = DATE_SUBTRACT(currentTimestamp, 12, "hour")

FOR w IN weather
  FILTER w.city == @city && w.date >= twelveHoursAgo
  SORT w.date DESC
  LIMIT 1
RETURN w
