LET currentTimestamp = DATE_NOW()
LET twelveHoursAgo = DATE_SUBTRACT(currentTimestamp, 12, "hour")

FOR r IN @@collection
  FILTER r.label == @label && r.date >= twelveHoursAgo
  SORT r.date DESC
  LIMIT 1
RETURN r
